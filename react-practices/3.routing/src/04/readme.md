# ex04: React Router Basic V: Link VS NavLink
> component/main.js

+ NavLink
    + 네비 안에서 선택된거에 대한 css 처리를 할 수 있음 
        ```css
        li a {color:#000}
        li a.active {color:#f00}
        ```
